name: jobs examples
on:
    push      
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: exemplo de um comando inválido
              run: ecko Hello, world! Build Failed.
    deploy:
        runs-on: ubuntu-latest
        needs: build
        steps:
            - name: step do job2
              run: echo Hello, world! Job2.
